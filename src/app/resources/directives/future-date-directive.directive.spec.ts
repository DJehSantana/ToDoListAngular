import { FutureDateDirectiveDirective } from './future-date-directive.directive';

describe('FutureDateDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new FutureDateDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
